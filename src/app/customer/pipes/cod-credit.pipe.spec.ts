import { CodCreditPipe } from './cod-credit.pipe';

describe('CodCreditPipe', () => {
  it('create an instance', () => {
    const pipe = new CodCreditPipe();
    expect(pipe).toBeTruthy();
  });
});
