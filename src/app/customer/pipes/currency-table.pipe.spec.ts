import { CurrencyTablePipe } from './currency-table.pipe';

describe('CurrencyTablePipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyTablePipe();
    expect(pipe).toBeTruthy();
  });
});
